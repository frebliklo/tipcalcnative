import React, { Component } from 'react'
import { Text } from 'react-native'

import ScrollWrapper from '../components/ScrollWrapper'

class Sales extends Component {
  render() {
    return (
      <ScrollWrapper>
        <Text>This is the sales screen!</Text>
      </ScrollWrapper>
    )
  }
}

export default Sales
